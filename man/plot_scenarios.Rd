% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_scenarios.R
\name{plot_scenarios}
\alias{plot_scenarios}
\title{Plot Simulated Symbiotic Scenarios}
\usage{
plot_scenarios(
  mechanisms = c("Nutritional", "Defensive", "Neutral", "FreeRider"),
  n = 100,
  noise = 0.15,
  seed = NULL,
  facet_by = c("wrap", "grid"),
  return_data = FALSE
)
}
\arguments{
\item{mechanisms}{A character vector of symbiotic mechanisms to simulate. Defaults to all four.}

\item{n}{Number of hosts to simulate per mechanism × condition (default = 100).}

\item{noise}{Standard deviation of noise to add to host fitness (default = 0.15).}

\item{seed}{Optional integer for reproducibility.}

\item{facet_by}{One of "wrap" or "grid". "wrap" facets by mechanism only. "grid" facets by condition × mechanism.}

\item{return_data}{Logical. If TRUE, returns the full simulated dataset instead of the plot. Default = FALSE.}
}
\value{
A ggplot object (or a tibble if return_data = TRUE).
}
\description{
Visualizes simulated host-symbiont data under various mechanistic archetypes
and stress conditions, producing a clean, publication-ready multi-panel plot.
}
\details{
This function serves as a powerful visualization tool to compare the conceptual
fingerprints of different symbiotic mechanisms. The `facet_by = 'wrap'` option
provides a compact view, while `facet_by = 'grid'` creates a stricter
matrix comparing every mechanism across both stress conditions.
}
\examples{
# Basic usage to see all scenarios
plot_scenarios()

# Use a grid layout for a more structured comparison
plot_scenarios(facet_by = "grid")

# Generate the data for custom analysis instead of plotting
scenario_df <- plot_scenarios(return_data = TRUE)
}
