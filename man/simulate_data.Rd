% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{simulate_data}
\alias{simulate_data}
\title{Simulate Symbiotic Interaction Data}
\usage{
simulate_data(
  mechanism = c("Nutritional", "Defensive", "Neutral", "FreeRider", "Custom"),
  n = 150,
  noise = 0.15,
  is_stress = FALSE,
  verbose = TRUE,
  seed = NULL,
  custom_params = NULL,
  add_label = FALSE,
  include_id = TRUE,
  return_invisible = FALSE
)
}
\arguments{
\item{mechanism}{A character string specifying the symbiotic mechanism.}

\item{n}{A single positive integer for the number of hosts to simulate.}

\item{noise}{A single non-negative numeric value for the noise standard deviation.}

\item{is_stress}{A logical value (TRUE/FALSE) for the stress condition.}

\item{verbose}{A logical value. If TRUE, prints simulation parameters.}

\item{seed}{An optional integer to set the random seed for reproducibility.}

\item{custom_params}{An optional list with `intercept` and `slope`.}

\item{add_label}{A logical value. If TRUE, adds a human-readable, ordered factor column.}

\item{return_invisible}{A logical value. If TRUE, the function returns the
data frame invisibly, preventing auto-printing in the console.}
}
\value{
A tibble with host-symbiont data. The ground truth parameters of the
  simulation are stored as an attribute named "params".
}
\description{
This is the core simulation engine for the mhgsiR package. It generates a
dataset of host fitness based on symbiont abundance under one of four
core mechanistic archetypes.
}
\details{
(The excellent philosophy text you wrote remains here...)
}
\examples{
# Simulate and pipe directly to a plot without printing the data
simulate_data(mechanism = "Defensive", seed = 42, return_invisible = TRUE) \%>\%
  ggplot2::ggplot(aes(x = abundance, y = fitness)) +
  ggplot2::geom_point()
}
